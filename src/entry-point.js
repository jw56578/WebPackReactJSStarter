import React from 'react';
import ReactDOM from 'react-dom';
//change this to whatever you want to test at the moment
//maybe just read the query string and conditionally render app so you don't have to keep checking out this file
import App from './components/app';


ReactDOM.render(<div><App /></div>, document.getElementById('app'));